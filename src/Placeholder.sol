// SPDX-License-Identifier: MIT
pragma solidity 0.8.24;

/// @title Placeholder
/// @notice Smoke-test contract for Fase 0 Foundry scaffold. Removed in later phases.
contract Placeholder {
    /// @notice Returns a constant ping value for smoke tests.
    /// @return Always `1`.
    function ping() external pure returns (uint256) {
        return 1;
    }
}
